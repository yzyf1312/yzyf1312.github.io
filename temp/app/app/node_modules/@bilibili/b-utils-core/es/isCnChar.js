import { RE_CN_CHAR } from './consts';

/**
 * 判断一个变量是不是由汉字组成
 * @param {*} value
 *
 * @example
 * isCnChar('哔哩哔哩');  // => true
 * isCnChar('bilibili'); // => false
 */
function isCnChar(value) {
  return RE_CN_CHAR.test(value);
}

export default isCnChar;
