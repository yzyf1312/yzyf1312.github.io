import { RE_EMAIL } from './consts';

/**
 * 判断一个变量是不是邮箱地址
 * @param {*} value
 *
 * @example
 * isEmail('abc@bilibili.com); // => true
 * isEmail('abc.bilibili.com); // => false
 */
function isEmail(value) {
  return RE_EMAIL.test(value);
}

export default isEmail;
