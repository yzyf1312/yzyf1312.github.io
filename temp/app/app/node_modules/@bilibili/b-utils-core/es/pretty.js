import toRowType from './toRowType';

/**
 * 格式化值的输出为字符串模板
 * @param {*} value
 * @returns {string}
 */
function pretty(value) {
  if (typeof value === 'object' && toRowType(value) !== 'Symbol') {
    return JSON.stringify(value, null, 2);
  }

  return String(value);
}

export default pretty;
