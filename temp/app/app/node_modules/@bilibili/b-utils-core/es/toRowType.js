/**
 * 获取变量toString后去掉中括号和object的值
 * @param {*} value
 * @returns
 *
 * toRowType([]);  // => 'Array'
 * toRowType({});  // => 'Object'
 */
function toRowType(value) {
  return Object.prototype.toString.call(value).slice(8, -1);
}

export default toRowType;
