import { RE_URL } from './consts';

/**
 * 判断一个变量是不是url链接
 * @param {*} value
 *
 * @example
 * isUrl('https://www.bilibili.com'); // => true
 */
function isUrl(value) {
  return RE_URL.test(value);
}

export default isUrl;
