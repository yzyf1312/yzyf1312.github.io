import { BUTILS_CACHE } from './consts';

/**
 * 移除缓存数据
 * @param {string} key
 */
function removeCache(key) {
  var caches = window[BUTILS_CACHE] || {};
  delete caches[key];
}

export default removeCache;
