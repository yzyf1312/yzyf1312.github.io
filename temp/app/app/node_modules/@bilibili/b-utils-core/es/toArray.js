import isArr from './isArr';

/**
 * 任意类型转数组类型
 * @param value
 *
 * @example
 * toArray(123);   // => [ 123 ]
 * toArray([ 123 ]); // => [ 123 ]
 */
function toArray(value) {
  return isArr(value) ? value : [value];
}

export default toArray;
