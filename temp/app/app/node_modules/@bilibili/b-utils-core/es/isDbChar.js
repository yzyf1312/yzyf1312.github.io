import { RE_DB_CHAR } from './consts';

/**
 * 判断一个变量是不是由双字节字符组成
 * @param {*} value
 *
 * @example
 * isDbChar('哔哩哔哩');  // => true
 * isDbChar('bilibili'); // => false
 */
function isDbChar(value) {
  return RE_DB_CHAR.test(value);
}

export default isDbChar;
