'use strict';

/**
 * 移除LocalStorage数据
 * @param {string} key
 * @returns
 */
function removeLocalStorage(key) {
  try {
    return localStorage.removeItem(key);
  } catch (err) {
    return null;
  }
}

module.exports = removeLocalStorage;
