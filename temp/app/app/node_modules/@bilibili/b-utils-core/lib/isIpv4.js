'use strict';

var consts = require('./consts');

/**
 * 判断一个变量是不是一个Ipv4地址
 * @param {*} value
 *
 * @example
 * isIpv4('127.0.0.1');  // => true
 * isIpv4('::1/128');    // => false
 */
function isIpv4(value) {
  return consts.RE_IPV4.test(value);
}

module.exports = isIpv4;
