'use strict';

var consts = require('./consts');

/**
 * 判断一个变量是不是由双字节字符组成
 * @param {*} value
 *
 * @example
 * isDbChar('哔哩哔哩');  // => true
 * isDbChar('bilibili'); // => false
 */
function isDbChar(value) {
  return consts.RE_DB_CHAR.test(value);
}

module.exports = isDbChar;
