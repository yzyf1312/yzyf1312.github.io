'use strict';

var consts = require('./consts');

/**
 * 判断一个变量是不是url链接
 * @param {*} value
 *
 * @example
 * isUrl('https://www.bilibili.com'); // => true
 */
function isUrl(value) {
  return consts.RE_URL.test(value);
}

module.exports = isUrl;
