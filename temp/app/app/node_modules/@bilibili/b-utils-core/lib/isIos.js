'use strict';

var _isIos;

/**
 * 根据UA判断是否为iOS系统
 * @returns
 */
function isIos() {
  if (_isIos === undefined) {
    _isIos = /\(i[^;]+;( U;)? CPU.+Mac OS X/i.test(navigator.userAgent);
  }

  return _isIos;
}

module.exports = isIos;
