'use strict';

function _interopDefault (ex) { return (ex && (typeof ex === 'object') && 'default' in ex) ? ex['default'] : ex; }

var base64ToUnit8Array = _interopDefault(require('./base64ToUnit8Array'));

/**
 * Base64或dataURI转File对象
 * @param {*} base64 Base64或dataURI数据
 * @param {*} fileName 文件名称
 * @param {*} mimeType 文件类型
 * @returns {File}
 */
function base64ToFile(base64, fileName, mimeType) {
  mimeType = mimeType || /:(.*?);/.test(base64) && RegExp.$1;
  var unit8array = base64ToUnit8Array(base64);
  return new File([unit8array], fileName, {
    type: mimeType
  });
}

module.exports = base64ToFile;
