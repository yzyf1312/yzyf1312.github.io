'use strict';

var _isZhTw;

/**
 * 判断是用否繁体字显示
 * @returns {boolean}
 */
function isZhTw() {
  if (_isZhTw === undefined) {
    var ua = navigator.userAgent.toLowerCase();
    _isZhTw = /c_locale\/zh(-|_)(hant|tw|mo|hk)/.test(ua);
  }

  return _isZhTw;
}

module.exports = isZhTw;
