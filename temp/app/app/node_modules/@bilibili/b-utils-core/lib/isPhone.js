'use strict';

var consts = require('./consts');

/**
 * 判断一个变量是不是手机号
 * @param {*} value
 *
 * @example
 * isPhone('13570347570');  // => true
 * isPhone('2471150552');   // => false
 */
function isPhone(value) {
  return consts.RE_PHONE.test(value);
}

module.exports = isPhone;
