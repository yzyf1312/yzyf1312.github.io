'use strict';

/**
 * 生成一个由参数元组构组成的数组
 *
 * @example
 *
 * const colors = tulpeArray('blue', 'pink');  // => ['blue', 'pink']
 * const ColorType = ElementOf<typeof colors>; // => 'blue' | 'pink'
 */
function tulpeArray() {
  return [].slice.call(arguments);
}

module.exports = tulpeArray;
