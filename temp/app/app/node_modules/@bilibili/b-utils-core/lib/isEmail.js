'use strict';

var consts = require('./consts');

/**
 * 判断一个变量是不是邮箱地址
 * @param {*} value
 *
 * @example
 * isEmail('abc@bilibili.com); // => true
 * isEmail('abc.bilibili.com); // => false
 */
function isEmail(value) {
  return consts.RE_EMAIL.test(value);
}

module.exports = isEmail;
