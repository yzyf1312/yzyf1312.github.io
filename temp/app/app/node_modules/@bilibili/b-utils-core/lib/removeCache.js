'use strict';

var consts = require('./consts');

/**
 * 移除缓存数据
 * @param {string} key
 */
function removeCache(key) {
  var caches = window[consts.BUTILS_CACHE] || {};
  delete caches[key];
}

module.exports = removeCache;
